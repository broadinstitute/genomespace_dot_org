<p class="footer"><% if user_signed_in? %>
	<%= current_user.cms_admin? ? link_to("Admin", pages_path) : nil %> | <%= link_to "Team", view_deliverables_path %> | <%= link_to "Logout", destroy_user_session_path, :method => "delete" %>  
<% else %>
	<%= link_to "Login", main_app.new_user_session_path %> 
<% end %></p>